#!/bin/bash -e

dir=/var/tmp/test-restore-$USER
rm -rf "$dir"
mkdir -p "$dir"

for backup_path in ~/.duply/*; do
    backup_name=${backup_path##*/}
    duply "$backup_name" restore "$dir"/"$backup_name"
done
