#!/bin/bash -ex

# Hee hee, chamone. Most builds are I/O limited, so assume one out of every 8
# CPUs may be idle. Number determined by Very Scientific Science.

procs=$(getconf _NPROCESSORS_ONLN)
[[ $procs ]] || exit

exec make -j "$(( procs + (procs/8) ))" "$@"
