#!/bin/bash -e

# Strip metadata from file for Flickr

: "${1?}"

exiftool "$@" -overwrite_original -XMP:all= -EXIF:all= -tagsfromfile @ -EXIF:{Make,Model,Orientation,FNumber,ShutterSpeedValue,DateTimeOriginal,FocalLength,ISO,ShutterSpeed} -XMP:{ShutterSpeedValue,ISO,ShutterSpeed}
