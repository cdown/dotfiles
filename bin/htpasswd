#!/bin/bash

# yes, apr1 is shit. don't worry, i don't store anything important here, just
# don't want people taking my bandwidth downloading stuff.
#
# one day linux will get bcrypt in crypt() :-(

echo "cdown:$(openssl passwd -apr1 "${1?missing pass}")"
