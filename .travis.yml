language: haskell

install:
  - cabal install shellcheck

before_script:
  - export PATH=$PATH:$HOME/.cabal/bin/

script:
  - shellcheck -e SC1090,SC1091 -s bash .bashrc .bash_profile
  - find .config/shell -not -name dircolors -type f -exec shellcheck -e SC1090,SC1091 -s sh {} +
  - find bin -not -name '*.gpg' -not -name view_attachment -type f -exec shellcheck -e SC1090,SC1091 -s bash {} +
  - '[[ -z "$(comm -23 <(bin/generate-gpg-gitignore) .gitignore)" ]]'

matrix:
  fast_finish: true

# Enable container based Travis CI infrastructure
# http://docs.travis-ci.com/user/migrating-from-legacy/
sudo: false
